export interface IInfoPage {
    page: number;
    pages: number;
    total: number;
    itemsPage: number;
}

// Definición principal de lo que es la consulta
export interface IResultData {
    listKey: string;
    definitionKey: string;
}
